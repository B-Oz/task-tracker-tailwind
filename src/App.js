import { Layout } from "./components";

const App = () => {
  return (
    <div className="w-full min-h-screen flex justify-center items-start bg-blue-100">
      <Layout />
    </div>
  );
};

export default App;
